telegram:
  botkey: ""  # set via TELEGRAM_BOT_KEY env var

server:
  enabled: true
  addr: "0.0.0.0"
  port: "8080"
  adminlogin: ""     # set via ADMIN_LOGIN env var
  adminpassword: ""  # set via ADMIN_PASSWORD env var
  jwtsecret: ""      # set via JWT_SECRET env var

database:
  type: "sqlite"
  path: "data/trash.db"
